<form method="post" action="{{ path('app_utilisateur_delete', {'id_u': utilisateur.id_u}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ utilisateur.id_u) }}">
    <button class="btn">Delete</button>
</form>
